<%= form_for(@timesheet) do |f| %>
  <% if @timesheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timesheet.errors.count, "error") %> prohibited this timesheet from being saved:</h2>
      <ul>
      <% @timesheet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :account %> <%= collection_select :timesheet, :account_id, @accounts, :id, :name, :include_blank => true %></p>

  <p><%= f.label :day %><%= f.date_select :day %></p>
  <%= f.submit %>
<% end %>
