<ul class="toolbar h-menu">
  <li><%= link_to 'New', new_user_path, :class => 'button' %></li>
  <li><%= link_to 'Delete', new_user_path, :class => 'button' %></li>
</ul>

<ul>
  <% @users.each do |u| %>
    <li><%= link_to "#{u.first_name} #{u.last_name}", u %></li>
  <% end %>
</ul>
