<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :name %> <%= f.text_field :name, :autofocus => 'autofocus' %></p>
  <p><%= f.label :category %> <%= collection_select :account, :category_id, AccountCategory.all, :id, :name, :include_blank => true %></p>
  <p><%= f.label :parent_account %> <%= collection_select :account, :parent_id, @accounts, :id, :name, :include_blank => true %></p>
  <%= f.submit %>
<% end %>
